# Phase 1: desktop training

```powershell
.\gradlew.bat runDesktopTraining
```

Speakers stay off. To hear desktop tones in a quiet room:

```powershell
.\gradlew.bat run --args="--audio"
```

That is **not** Driver Hub validation.
