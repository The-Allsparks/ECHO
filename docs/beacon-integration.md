# BEACON integration

BEACON owns communication-health observation. ECHO does not diagnose links.

Optional field `beaconWarning` → `WARN_BEACON`. Missing BEACON ≠ healthy link; it means **no warning candidate from BEACON**.

Flag `beaconAdapter` default **false**.
